

<div class="modal fade" id="modal" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog">
        <dialog class="modal-content">
            <div class="modal-body">
                <div class="container-fluid">
                    <div class="row">
                    <div class="col-lg-12">
                        <form class="message-form">
                            <div class="form-group">
                                <label for="textarea">Write Your Message</label>
                                <textarea class="form-control" id="textarea" rows="3"></textarea>
                            </div>
                            <button type="submit" class="btn btn-default">Send Email</button>
                            <button type="close" class="btn btn-default">Close</button>
                        </form>
                    </div>
                    </div>
                </div>
            </div>
        </dialog>
    </div>
</div>
<section class="container-fluid">
    <form class="form-comtainer-centered">
        <div class="row justify-content-center">
            <!--<button id="search">TESTING</button>-->
            <ul>
                {{#each user}}
                    <li>
                    <img class="imgprofile" src="/img/fish.jpg"></img>
                    <div class="col-sm-12 col-md-6 col-lg-3 text-black mt-3 text-justify">
                        <h4>{{name}} The {{species}}</h4>
                        <h4>Age: {{age}}</h4>
                        <button data-id="{{id}}" id="message" type="button" class="btn btn-success">Message me</button>
                    </div>
                    </li>
                {{/each}}
            </ul>
            <!--
            
            <div class="col-sm-12 col-md-6 col-lg-3 text-black mt-3 text-justify">
                <img class="imgprofile" src="img/pexels-dinuka-sasith-1806769.jpg">
                <h4>Rocky</h4>
                <button type="button" class="btn btn-success">Message me</button>
            </div>
            <div class="col-sm-12 col-md-6 col-lg-3 text-black mt-3 text-justify">
                <img class="imgprofile" src="img/pexels-milica-popovic-1573919.jpg">
                <h4>Pat</h4>
                <button type="button" class="btn btn-success">Message me</button>
            </div>
            -->
        </div>
    </form>
</section>
<script>
    $(document).ready(() => {
        /*
        $(".message").on("click", function(event){
            const id = $(this).data("id");
            $("#modal").attr("data-id",id);
            $('#modal').modal('show');
        });
        $("form.message-form").on("close", function(event){
            $('#modal').modal("hide");
        });
        $("form.message-form").on("submit", function(event){
            $("#modal").modal("hide");
            let id = $("#modal").data("id");
            if(id == null) id = 1;
            $.post("/api/send-email",{
                id: id,
                subject: "PetMatch - Message from a friend",
                text: $("#textarea").text
            }).then(function(){
                console.log("Email Successfully Sent!");
            });
        });
        */
        $("#backimage").css("background-image", "url('img/Collagelight.png')");
        $("#search").on("click", function(event){
            //event.preventDefault();
            $.ajax("/api/searchAll",{
                type: "POST",
                data: data
            }).then(() => {
                console.log("HERE1");
                location.reload();
            });
        });
    });
</script>